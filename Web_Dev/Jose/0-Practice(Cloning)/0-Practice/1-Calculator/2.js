const num_one = document.getElementById('num-one');
const num_two = document.getElementById('num-two');

const resu = document.getElementById('res');

function out(result){
  resu.textContent=result;
}
